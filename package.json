{
  "name": "calculator",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "bun test",
    "build": "bunx tsc --noEmit && bun build index.ts --outdir='./dist'"
  }
}